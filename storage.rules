rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Preserve existing public assets; paid binaries are server-access only.
    match /{prefix}/{remaining=**} {
      allow read: if prefix != 'pocket-paid';
      allow write: if false;
    }
  }
}
